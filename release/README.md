# Release artifacts

Upload `MyStremioSetup-v2.2.0_x64.exe` to GitHub Releases. The installer is self-contained.

`SHA256SUMS.txt` is included for checksum verification. Installer binaries (`.exe`, `.zip`) are gitignored and must not be committed.
